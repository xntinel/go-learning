# Worker order — Chapter 48: Modern Go (Language and Stdlib)


Generate each lesson to the standard in `docs/01-quality-standard.md` and
`AGENT-ORDER-go-quality.md`. Prove gate-mode lessons with `challenges/tools/go_gate_append.py`
(real *_test.go + Example //Output + cmd/demo). Bar-mode lessons: clean `gofmt -l`,
honest Concepts, section 15 self-consistency; external/network code behind build tags.
No emojis. English. Verify every API on pkg.go.dev before using it.

| # | Lesson | Mode | Artifact / arc |
|---|--------|------|----------------|
| 1 | Directory-Confined Filesystem with os.Root | gate | Build a file server that uses os.OpenRoot to block path-traversal and symlink escape; test escape attempts are rejected. |
| 2 | Deterministic Concurrency with testing/synctest | gate | Test a timeout/retry component inside a synctest bubble with virtualized time; no flaky sleeps. |
| 3 | Value Interning with the unique Package | gate | Intern repeated strings/structs with unique.Make; benchmark memory and pointer-equality fast path. |
| 4 | Weak Pointers and runtime.AddCleanup | gate | Build a leak-free canonical-map cache with weak.Pointer plus runtime.AddCleanup; verify eviction. |
| 5 | Spawning Goroutines with sync.WaitGroup.Go | gate | Fan-out work with WaitGroup.Go; show the bug class it removes and the new waitgroup vet check. |
| 6 | Generic Type Aliases | gate | Define parameterized aliases (Set[T] = map[T]struct{}) and use them across an API for gradual refactor. |
| 7 | Tool Dependencies with go.mod tool Directives | gate | Pin and run a dev tool via go get -tool and go tool; replace the tools.go blank-import hack. |
| 8 | Excluding Directories with the go.mod ignore Directive | gate | Use the go.mod ignore directive to keep testdata/generated dirs out of ./... and all. |
| 9 | Structured Test Metadata with T.Attr and T.Output | gate | Emit structured key/value attributes and use T.Output for richer CI reporting. |
| 10 | Container-Aware GOMAXPROCS | gate | Show cgroup-aware default GOMAXPROCS and runtime.SetDefaultGOMAXPROCS to avoid CFS throttling. |
| 11 | Swiss-Table Map Internals | gate | Explain the 1.24 Swiss-table map rewrite; benchmark probe behavior and memory vs the old design. |
| 12 | Streaming JSON with encoding/json/v2 and jsontext | bar | Decode large streams with json/v2 and the jsontext token layer; contrast with v1 semantics. |
| 13 | Production Tracing with runtime/trace FlightRecorder | bar | Capture the last seconds before a rare event with runtime/trace.FlightRecorder. |
| 14 | Generic Error Matching with errors.AsType | bar | Unwrap typed errors with the generic errors.AsType; contrast with errors.As. |
| 15 | Initialized Allocation with new(expr) | bar | Use new(expr) to simplify optional-pointer patterns in config/serialization. |
| 16 | Code Modernization with go:fix and go fix | bar | Annotate deprecations with //go:fix inline and run go fix to migrate call sites. |
