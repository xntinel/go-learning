TASK: Rewrite the low-quality Go lessons in ONE chapter to the canonical standard.

CHAPTER TO PROCESS: 25-iterators-and-modern-go

STEP 1 — Read the standard (mandatory, do not skip):
- challenges/go/AGENT-ORDER-go-quality.md  (the full order; follow it exactly)
- challenges/CURRICULUM-METHODOLOGY.md sections 5, 14.1, 15, 0
- Gold reference: challenges/go/07-structs-and-methods/01-struct-declaration-and-initialization/01-struct-declaration-and-initialization.md

STEP 2 — Find the files to fix in THIS chapter only:
    grep -rl --include="*.md" "bloom_level:" challenges/go/25-iterators-and-modern-go/
Rewrite every file the command lists. Do NOT touch files it does not list. Do NOT rename or reorder files. Keep the `# N. Title` H1.

STEP 3 — For each file, do a real rewrite (NOT mechanical deletion) to the canonical shape:
  # N. Title / short intro / ## Concepts (### subsections) / ## Exercises / ## Common Mistakes / ## Verification / ## Summary / ## What's Next / ## Resources
Delete the HTML metadata block, ## Prerequisites, ## Learning Objectives. Never delete ## The Problem without migrating its code into ## Exercises first.

EXERCISE QUALITY (highest priority):
- The lesson is a real library package (e.g. `package server`), NOT `package main`.
- Verification is a real `*_test.go`: table-driven, t.Parallel(), sentinel errors wrapped with %w and asserted via errors.Is. NEVER main()+fmt.Printf+"Expected output".
- ALSO include an `Example` function with `// Output:` AND a runnable `cmd/demo/main.go` (uses only exported API; add small exported accessors if needed).
- ALL Go code indented with TABS, not spaces (gofmt -l must be empty).
- No emojis or decorative symbols anywhere (no checkmarks, stars, arrows-as-decoration). English only. Do not invent stdlib APIs.

STEP 4 — RUN THE GATE (mandatory; a lesson that you did not gate-check does not count as done):
For each rewritten lesson, extract its Go code into a temp module UNDER THE REPO and run the gate with the Go 1.26 toolchain:

    export PATH=/var/folders/c7/_6tpyby91bb74nzwr_wbj9380000gn/T/opencode/go1.26/bin:$PATH
    go version   # must print go1.26

    mkdir -p challenges/go/.verify/25-iterators-and-modern-go/<lesson>
    cd challenges/go/.verify/25-iterators-and-modern-go/<lesson>
    go mod init example.com/verify
    # write the lesson's .go files here (library files at root; the demo as cmd/demo/main.go)
    test -z "$(gofmt -l .)"   && echo GOFMT_OK
    go vet ./...              && echo VET_OK
    go build ./...            && echo BUILD_OK
    go test -count=1 -race ./... && echo TEST_OK

Every lesson must reach TEST_OK. If a lesson fails, FIX the lesson markdown and re-run until it passes. Delete the .verify dir when done. Do NOT write outside the repo (/tmp is sandbox-blocked and will stall you).

STEP 5 — Report: per-lesson PASS/FAIL, files touched, any broken ## What's Next links.

Process the ENTIRE chapter. Do not stop early.
