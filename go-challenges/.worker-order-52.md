# Worker order — Chapter 52: AI and LLM Backends in Go


Generate each lesson to the standard in `docs/01-quality-standard.md` and
`AGENT-ORDER-go-quality.md`. Prove gate-mode lessons with `challenges/tools/go_gate_append.py`
(real *_test.go + Example //Output + cmd/demo). Bar-mode lessons: clean `gofmt -l`,
honest Concepts, section 15 self-consistency; external/network code behind build tags.
No emojis. English. Verify every API on pkg.go.dev before using it.

| # | Lesson | Mode | Artifact / arc |
|---|--------|------|----------------|
| 1 | Calling LLMs with the Anthropic and OpenAI Go SDKs | bar | Make typed completion calls with the official Go SDKs; configure models and params. |
| 2 | Streaming Completions over SSE | bar | Stream tokens with server-sent events; incremental rendering and cancellation. |
| 3 | Tool and Function Calling | bar | Define tools, parse tool-use requests, execute, and feed results back to the model. |
| 4 | Embeddings and Vector Search with pgvector | bar | Generate embeddings and run similarity search in Postgres with pgvector and pgx. |
| 5 | Building a RAG Pipeline | bar | Chunk, embed, retrieve, and ground an answer; cite sources; manage context. |
| 6 | Building MCP Servers in Go | bar | Expose tools and resources to LLM clients with the Go MCP SDK. |
| 7 | Prompt Templating and Token Budgeting | bar | Template prompts safely and budget tokens; count and truncate context. |
| 8 | LLM Resilience: Retries, Timeouts, and Caching | bar | Add retries with jitter, timeouts, and response caching around flaky LLM calls. |
