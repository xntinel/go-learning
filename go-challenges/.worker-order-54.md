# Worker order — Chapter 54: Cloud-Native Platform: Containers, Kubernetes and Multi-Cloud


Generate each lesson to the standard in `docs/01-quality-standard.md` and
`AGENT-ORDER-go-quality.md`. Prove gate-mode lessons with `challenges/tools/go_gate_append.py`
(real *_test.go + Example //Output + cmd/demo). Bar-mode lessons: clean `gofmt -l`,
honest Concepts, section 15 self-consistency; external/network code behind build tags.
No emojis. English. Verify every API on pkg.go.dev before using it.

| # | Lesson | Mode | Artifact / arc |
|---|--------|------|----------------|
| 1 | Driving Docker with the Engine SDK | bar | Build, run, and stream logs from containers with the Docker Engine Go SDK. |
| 2 | Building a Kubernetes Operator with kubebuilder | bar | Scaffold a CRD and operator with kubebuilder; define spec/status. |
| 3 | Reconcile Loops with controller-runtime | bar | Implement an idempotent reconcile loop with controller-runtime; watches and requeue. |
| 4 | Event-Driven Autoscaling with KEDA | bar | Scale a Go workload on queue depth with a KEDA ScaledObject. |
| 5 | Helm Packaging and GitOps Delivery | bar | Package a Go service as a Helm chart and deliver it via GitOps. |
| 6 | Portable Blob Storage with gocloud.dev | bar | Read/write blobs portably across S3/GCS/Azure with the gocloud.dev/blob API. |
| 7 | Portable Config and Secrets with gocloud | bar | Load config and secrets portably with gocloud runtimevar and secrets. |
| 8 | Distributed Caching with Redis | bar | Cache-aside and write-through with go-redis; TTLs, stampede protection. |
| 9 | Distributed Locks with Redis and redsync | bar | Acquire and renew Redlock-style distributed locks with redsync; fencing tokens. |
| 10 | Distributed Rate Limiting with Redis | bar | Implement a sliding-window/token-bucket limiter backed by Redis. |
