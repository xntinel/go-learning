# Worker order — Chapter 49: Application Security, Crypto and Supply Chain


Generate each lesson to the standard in `docs/01-quality-standard.md` and
`AGENT-ORDER-go-quality.md`. Prove gate-mode lessons with `challenges/tools/go_gate_append.py`
(real *_test.go + Example //Output + cmd/demo). Bar-mode lessons: clean `gofmt -l`,
honest Concepts, section 15 self-consistency; external/network code behind build tags.
No emojis. English. Verify every API on pkg.go.dev before using it.

| # | Lesson | Mode | Artifact / arc |
|---|--------|------|----------------|
| 1 | Post-Quantum Hybrid TLS with crypto/mlkem | gate | Negotiate X25519MLKEM768 in crypto/tls; verify the hybrid key exchange end to end. |
| 2 | Authenticated Encryption with AES-GCM and ChaCha20-Poly1305 | gate | Encrypt/decrypt with AEAD ciphers; correct nonce handling and associated data. |
| 3 | Envelope Encryption: KEK and DEK | gate | Implement KEK-wraps-DEK envelope encryption with key rotation; test unwrap-after-rotate. |
| 4 | Password Hashing with Argon2 and bcrypt | gate | Hash and verify passwords with argon2id and bcrypt; tune cost; constant-time compare. |
| 5 | Secure Tokens: PASETO vs JWT | bar | Issue and verify v4 PASETO; contrast with JOSE alg-confusion footguns. |
| 6 | OAuth2 and OIDC Authentication Flows | bar | Implement authorization-code-with-PKCE and validate OIDC ID tokens. |
| 7 | Secrets Management with Vault | bar | Fetch dynamic secrets from Vault via the Go API; lease renewal and revocation. |
| 8 | FIPS 140-3 Mode and the Go Cryptographic Module | bar | Build with GODEBUG=fips140 and the Go Cryptographic Module; verify enforced algorithms. |
| 9 | Vulnerability Scanning with govulncheck | bar | Gate CI on govulncheck reachability analysis; triage reachable vs unreachable CVEs. |
| 10 | Supply-Chain Security: SLSA Provenance and SBOMs | bar | Generate build provenance and an SBOM; map to SLSA levels. |
| 11 | Keyless Artifact Signing with Sigstore and cosign | bar | Sign and verify artifacts keylessly with cosign, Fulcio, and Rekor. |
