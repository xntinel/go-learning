# Worker order — Chapter 51: RPC and API Design


Generate each lesson to the standard in `docs/01-quality-standard.md` and
`AGENT-ORDER-go-quality.md`. Prove gate-mode lessons with `challenges/tools/go_gate_append.py`
(real *_test.go + Example //Output + cmd/demo). Bar-mode lessons: clean `gofmt -l`,
honest Concepts, section 15 self-consistency; external/network code behind build tags.
No emojis. English. Verify every API on pkg.go.dev before using it.

| # | Lesson | Mode | Artifact / arc |
|---|--------|------|----------------|
| 1 | Building Services with ConnectRPC | bar | Define a Connect service callable by gRPC clients and curl over HTTP/1.1. |
| 2 | REST/JSON Gateways with grpc-gateway | bar | Expose a protobuf service as REST/JSON via grpc-gateway annotations. |
| 3 | Protobuf Schemas and the buf Workflow | bar | Lint, breaking-change-check, and generate with buf; manage a schema module. |
| 4 | Schema-First GraphQL with gqlgen | bar | Generate resolvers from a GraphQL schema with gqlgen; wire a query and mutation. |
| 5 | Solving N+1 with GraphQL Dataloaders | bar | Batch and cache resolver fetches with dataloaders to kill the N+1 problem. |
| 6 | API Versioning Strategies | bar | Compare URI, header, and schema-evolution versioning; design a deprecation path. |
| 7 | Choosing Between REST, gRPC, and Connect | bar | Decision framework: payloads, streaming, browser reach, tooling, observability. |
