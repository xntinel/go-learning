# Worker order — Chapter 50: Messaging and Event-Driven Backends


Generate each lesson to the standard in `docs/01-quality-standard.md` and
`AGENT-ORDER-go-quality.md`. Prove gate-mode lessons with `challenges/tools/go_gate_append.py`
(real *_test.go + Example //Output + cmd/demo). Bar-mode lessons: clean `gofmt -l`,
honest Concepts, section 15 self-consistency; external/network code behind build tags.
No emojis. English. Verify every API on pkg.go.dev before using it.

| # | Lesson | Mode | Artifact / arc |
|---|--------|------|----------------|
| 1 | Kafka Clients with franz-go | bar | Produce and consume with franz-go; consumer groups and partitioning. |
| 2 | Kafka Transactions and Exactly-Once Semantics | bar | Use Kafka transactions for read-process-write exactly-once-in-stream. |
| 3 | Durable Messaging with NATS JetStream | bar | Publish/consume durable streams with the jetstream API; acks and replay. |
| 4 | Redis Streams and Consumer Groups | bar | Build a consumer group over Redis Streams with XADD/XREADGROUP/XACK and pending handling. |
| 5 | Event-Driven Pipelines with Watermill | bar | Compose pub/sub middleware pipelines with Watermill across backends. |
| 6 | The Transactional Outbox Pattern | gate | Implement the outbox over SQLite: write domain row plus outbox row in one tx; relay publishes. |
| 7 | Idempotent Consumers and the Inbox Pattern | gate | Dedupe at-least-once delivery with an inbox table keyed by message id. |
| 8 | Durable Execution with Temporal | bar | Model a saga as a Temporal workflow with activities and compensation. |
| 9 | Transactional Job Queues with River | bar | Enqueue jobs in the same Postgres tx as domain data with River; workers and retries. |
| 10 | Dead-Letter Queues and Retry Topologies | bar | Design DLQ and retry/backoff topologies for poison messages. |
