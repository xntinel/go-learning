# Worker order — Chapter 53: WebAssembly and Extensibility


Generate each lesson to the standard in `docs/01-quality-standard.md` and
`AGENT-ORDER-go-quality.md`. Prove gate-mode lessons with `challenges/tools/go_gate_append.py`
(real *_test.go + Example //Output + cmd/demo). Bar-mode lessons: clean `gofmt -l`,
honest Concepts, section 15 self-consistency; external/network code behind build tags.
No emojis. English. Verify every API on pkg.go.dev before using it.

| # | Lesson | Mode | Artifact / arc |
|---|--------|------|----------------|
| 1 | Embedding a Wasm Runtime with wazero | gate | Instantiate and call a Wasm module from Go with wazero; no cgo. |
| 2 | Host/Guest ABI and Linear Memory | gate | Pass strings/bytes across the host/guest boundary through linear memory; manage allocation. |
| 3 | A Wasm-Based Plugin System | gate | Load untrusted plugins as Wasm with a capability-scoped host API; sandbox enforcement. |
| 4 | Compiling Guest Modules with TinyGo and WASI | bar | Build WASI guest modules with TinyGo and run them on a host runtime. |
| 5 | Process Plugins with hashicorp/go-plugin | bar | Build out-of-process plugins over gRPC with hashicorp/go-plugin. |
| 6 | Embedding a Scripting Engine | bar | Embed a scripting/expression engine for user-defined logic; sandbox and limits. |
